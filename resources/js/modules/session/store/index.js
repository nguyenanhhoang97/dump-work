import * as getters from "./getters";
import * as actions from "./actions";

const state = {
  profile: []
};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
